[project]
name = "example-agent"
version = "0.1.0"
description = "Example AI Agent"
requires-python = ">=3.12"
dependencies = [
    # 필요한 의존성을 여기에 추가
    # 예: "openai>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
