[project]
name = "ai-common"
version = "0.1.0"
description = "Core, agent, and MCP helpers shared across the platform"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["core_common", "agent_common", "mcp_common"]
